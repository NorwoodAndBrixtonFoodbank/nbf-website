alter view audit_log_plus set (security_invoker = true);

alter view clients_plus set (security_invoker = true);

alter view completed_parcels set (security_invoker = true);

alter view family_count set (security_invoker = true);

alter view last_status set (security_invoker = true);

alter view parcels_plus set (security_invoker = true);

alter view reports set (security_invoker = true);

alter view users_plus set (security_invoker = true);
